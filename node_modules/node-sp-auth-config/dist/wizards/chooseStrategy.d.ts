import * as inquirer from 'inquirer';
import { IAuthContext, IAuthConfigSettings } from '../interfaces';
declare const wizard: (authContext: IAuthContext, answersAll?: inquirer.Answers, settings?: IAuthConfigSettings) => Promise<inquirer.Answers>;
export default wizard;
