var spsave = require('./../lib/src/index').spsave;
var fs = require('fs');

    var opts  = {
      username: 'admin@DevProj.onmicrosoft.com',
      password: 'Password123',
      siteUrl: 'https://devproj.sharepoint.com/sites/teamplannerdev-ses',
      glob: ['index.1.js'],
      folder: 'SiteAssets',
      notification: true
    };

function save(opts){
  spsave(opts)
  .then(function (data) {
    console.log('success');
  })
  .catch(function(err){
    if(err.statusCode){
    console.log('WOW error((: ' + err.statusCode);
    } else {
      console.log(err);
    }
  });
}

save(opts);
